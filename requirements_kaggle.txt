# Colab specific requirements for Google Colab Environment
# PyTorch is already installed natively in Colab (torch>=2.0.0)
# torchvision, torchaudio, numpy, matplotlib, scipy, psutil are also pre-installed

# Strict versions required for pipeline execution
# Core utilities
pydantic>=2.4
PyYAML>=6.0
tqdm>=4.65
Pillow>=10.0

# Evaluation
fastdtw>=0.3.4
similaritymeasures>=0.4.4

# Rendering
cairosvg>=2.7.1

# Optional (install only if missing)
psutil>=5.9